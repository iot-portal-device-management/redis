version_settings(constraint='>=0.22.2')

docker_build(
    'iotportaldevicemanagement-redis',
    '.',
    build_args = {'REDIS_USERNAME': 'iotportaldevicemanagement', 'REDIS_PASSWORD': 'REa73KiYig6h34iqakg'},
    dockerfile = 'Dockerfile.development',
)